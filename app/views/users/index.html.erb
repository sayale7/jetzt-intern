<% title "Users" %>

<table>
  <tr>
    <th>Username</th>
    <th>Email</th>
  </tr>
  <% for user in @users %>
    <tr>
      <td><%=h user.username %></td>
      <td><%=h user.email %></td>
	  <td><%= link_to "Löschen", :controller => "users", :action => "destroy_user", :id => user.id %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New User", new_user_path %></p>
